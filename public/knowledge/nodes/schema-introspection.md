# Schema introspection
Type: concept
Sources: https://justin.poehnelt.com/posts/rewrite-your-cli-for-ai-agents/

Runtime-queryable method signatures and types exposed by the CLI instead of relying on static docs.

## Relations
- → Discovery Document: "Under the hood, this uses Google’s Discovery Document with dynamic $ref resolution."
- ← Google Workspace CLI: "The better pattern: make the CLI itself the documentation, queryable at runtime."
