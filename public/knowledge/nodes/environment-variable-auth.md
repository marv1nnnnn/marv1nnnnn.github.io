# Environment variable auth
Type: concept
Sources: https://justin.poehnelt.com/posts/rewrite-your-cli-for-ai-agents/

Headless credential injection via environment variables for agent authentication.

## Relations
- ← Google Workspace CLI: "`GOOGLE_WORKSPACE_CLI_TOKEN` and `GOOGLE_WORKSPACE_CLI_CREDENTIALS_FILE` enable credential injection via environment"
